{
    "$schema": "http://json-schema.org/schema",
    "$id": "figma-component-importer",
    "title": "Figma component importer",
    "description": "imports a figma component as an angular component d.",
    "type": "object",
    "properties": {
        "project": {
            "description": "project where the components should be added to",
            "type": "string"
        },
        "token": {
            "description": "Your Figma API token.",
            "type": "string"
        },
        "componentPrefix": {
            "description": "the prefix to add to the components",
            "type": "string",
            "default": "figma-relay"
        }
    },
    "required": ["project"],
    "additionalProperties": false
}
